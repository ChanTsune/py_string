language: cpp

compiler:
  - clang
  - gcc

matrix:
  include:
    - os: linux
      install: "sudo apt install -qq libboost-all-dev"
    - os: osx
      install: "brew install boost"

script:
  - make -B googletest
