language: cpp

matrix:
  include:
    - os: linux
      compiler: clang
      install:
        - "sudo apt update -qq"
        - "sudo apt install -qq libboost-all-dev"
    - os: linux
      compiler: gcc
      install:
        - "sudo apt update -qq"
        - "sudo apt install -qq libboost-all-dev"
    - os: osx
      compiler: clang
    - os: osx
      compiler: gcc

script:
  - make -B test
